"""
Manipulate hadmin's configuration

hadmin's configuration exists in a directory structure like this:

    /
    |- queues/
    | |- staff.qa.yml
    | |- staff.fun.yml

The above configuration would create two queues, staff.qa and staff.fun.

"""
